# Excel to DRL Converter

This is a simple testing tool to convert a Drools Decision Table from Excel format to DRL.

This is useful for testing that the Decision Tables are being generated correctly.
